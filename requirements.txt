# QRZ Logbook Tools — Python dependencies
# 2026-03-19 Jim Carson - WT8P
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Or to create a virtual environment first:
#   python -m venv venv
#   source venv/bin/activate        # Linux / macOS
#   venv\Scripts\activate           # Windows
#   pip install -r requirements.txt

# HTTP requests — QRZ API calls
requests>=2.28.0

# Excel file reading — QRZ discrepancy report (.xlsx)
pandas>=1.5.0
openpyxl>=3.0.10
