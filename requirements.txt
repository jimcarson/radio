# adif_map.py and QRZ Logbook Tools requirements
# Install with: pip install -r requirements.txt

# Core — required by all scripts
requests>=2.28

# QSO data manipulation
pandas>=1.5
openpyxl>=3.0

# Map visualisation (adif_map.py)
folium>=0.14
pyyaml>=6.0

# Shapefile reading for adif_setup.py (Census TIGER / Natural Earth CDN)
pyshp>=2.3
